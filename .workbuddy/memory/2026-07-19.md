# 2026-07-19

## 接手完成 PRD 1.1（FR-8 外部知识库接入）
- 需求：把 WuDaDao 知识库位置从仓库硬编码改为 `.env` 的 `WUDADAO_KB_PATH` 配置（绝对路径或 file:// URL），不可达时明确降级，不静默失败；`/api/reload` 依据配置重建 RAG 索引。
- 改动：`app/src/wiki.js`（getWikiRoot + loadWiki 抛错）、新增 `app/rag/build.js`、重构 `app/rag/index.js`、`app/server.js`（降级 + meta 状态 + reload 重建 + 启动补建）、前端 `app.js`/`index.html`/`styles.css`（#kbStatus 状态位）、`.env.example`、`.gitignore` 忽略 `.env`、`REQUIREMENTS.md`/`DESIGN.md` 文档更新。
- 验证：默认路径 wikiCount=43 正常；坏路径 wikiStatus=degraded 且 /api/guide 仍规则生成（rag=false）无 500；file:// URL 正常加载 43 篇；reload 返回 ragRebuild=started。RAG 实际嵌入因环境密钥无效仅日志失败，降级链路正确。
- 决策记录见 `overview-fr8.md`；远程 HTTP 知识库拉取留待后续。

## 项目约定（已确认）
- 本仓库 `WuDaDao/` 已被 .gitignore 忽略（外部知识库，不入库）；`.env` 现也忽略，`.env.example` 为安全模板。
- 零 npm 依赖（NFR-1）；.env 用自写 loadDotEnv 解析，不引 dotenv。
