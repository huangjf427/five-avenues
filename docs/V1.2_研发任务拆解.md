# FiveAvenues V1.2 研发任务拆解（RAG 增强 + 社区运营增强）

> 版本：V1.2（**PR #2 已合并、Issue #1 已关闭**；A1~A6 随 PRD 合并采纳，研发阶段定稿）
> 阶段：阶段四·研发跟进（基于 `docs/PRD_V1.2_RAG社区增强.md` 拆解）
> 关联：PR #2（huangjf427 自审）、GitHub Issue #1（FR-8）、`REQUIREMENTS.md`
> 拆解原则：每条 FR 拆为可独立交付 / 可并行的前端、后端、数据子任务；依赖项标注前置；验收对齐 PRD §8。

---

## 0. 前置依赖与阻塞
| 前置 | 状态 | 阻塞影响 |
|---|---|---|
| FR-8 外部知识库接入（Issue #1） | ✅ 已关闭（并入 PR #2） | 已解除对 FR-9 / FR-10 的阻塞；RAG 可用前提现已明确 |
| A1 埋点上报端点 | 🟡 随 PRD 合并采纳（研发定稿） | 不再硬阻塞；落地 T-M.* 时由技术负责人定端点 |
| A3 敏感词词库来源 | 🟡 随 PRD 合并采纳（研发定稿） | 落地 T-13.3 时定词库来源（先静态词表占位） |
| A4 「有用」排序权重 | 🟡 随 PRD 合并采纳（研发定稿） | 落地 T-11.4 时定口径（先 1:1 叠加） |
| A5 存储结构（votes/reports 落点） | 🟡 随 PRD 合并采纳（研发定稿） | 落地 T-11.1 / T-12.1 时定，沿用 NFR-4 |

> 排期建议：PR #2 已合并、Issue #1 已关闭，前置阻塞解除；P1 先并行 FR-9 / FR-11 / FR-12；P2 的 FR-10 / FR-13 待 FR-12 完成后启动（A3/A4 转入研发定稿，不再硬阻塞）。

---

## 1. FR-9 RAG 配置引导与缺省体验（P1）

| 任务 ID | 类型 | 描述 | 依赖 | 验收（PRD §8） |
|---|---|---|---|---|
| T-9.1 | 后端 | `meta.ragEnabled` 已存在；新增"配置缺失原因/指引"字段（未配密钥 / 索引缺失），供前端展示 | FR-1、FR-8 | meta 暴露可用状态与原因 |
| T-9.2 | 前端 | 定制行程页展示 RAG 状态徽标；未配置时「AI 增强生成」置灰 + 配置指引入口（触发 `rag_config_view` 埋点） | T-9.1 | 置灰且点击触发埋点 |
| T-9.3 | 后端/降级 | RAG 勾选后失败 → 降级规则生成 + 提示（沿用 FR-1，验证 `guide.rag=false` 且不 500） | FR-1、FR-8 | 降级不报 500 且提示切换 |

---

## 2. FR-10 行程生成溯源展示（P2）

| 任务 ID | 类型 | 描述 | 依赖 | 验收 |
|---|---|---|---|---|
| T-10.1 | 前端 | RAG 生成时渲染 `sources` 区块，点击展开片段 | FR-9、FR-1 `sources` 字段 | 溯源可展开 |
| T-10.2 | 前端 | 规则生成展示"规则推荐"说明（非逐条溯源） | FR-1 | 规则态有说明 |

---

## 3. FR-11 评价「有用」互动投票（P1）

| 任务 ID | 类型 | 描述 | 依赖 | 验收 |
|---|---|---|---|---|
| T-11.1 | 数据模型 | `reviews.json` 扩展 `helpfulCount` + `helpfulBy[]`（或独立 votes 结构，落点待 A5） | FR-3、FR-6 | 模型可存投票 |
| T-11.2 | 后端 API | `POST /api/reviews/:id/helpful` 切换投票（登录校验、同用户去重、计数 ±1） | T-11.1、FR-6 | 去重、可取消 |
| T-11.3 | 前端 | 评价卡「有用 👍 (n)」按钮；匿名仅可见计数 | T-11.2、FR-3 | 交互 + 匿名可见计数 |
| T-11.4 | 后端/排序 | 公开列表引入"有用"加权（与 `feedback.json` 既有加权协调，口径待 A4） | T-11.2、FR-3 | 参与排序 |

---

## 4. FR-12 评价举报（P1）

| 任务 ID | 类型 | 描述 | 依赖 | 验收 |
|---|---|---|---|---|
| T-12.1 | 数据模型 | reports 结构（reviewId, reason, reporterId, status, createdAt） | FR-3、FR-4 | 模型就绪 |
| T-12.2 | 后端 API | `POST /api/reviews/:id/report`（登录校验、原因必填、10 分钟内同评价重复拦截） | T-12.1、FR-6 | 空原因 / 重复被拒 |
| T-12.3 | 前端 | 已通过评价卡「举报」入口 + 原因选择弹窗 | T-12.2、FR-3 | 交互闭环 |
| T-12.4 | 隐私 | 举报人不向被举报者泄露身份 | T-12.2 | 身份隔离 |

---

## 5. FR-13 审核工作台增强（P2）

| 任务 ID | 类型 | 描述 | 依赖 | 验收 |
|---|---|---|---|---|
| T-13.1 | 后端 API | 举报工单处理：uphold（驳回举报→评价维持）/ dismiss（采纳→评价转 rejected） | FR-4、FR-12 | 两种决定均记录操作人/时间 |
| T-13.2 | 后端 API | 批量通过 / 拒绝评价（单条失败不中断整批，汇总失败） | FR-4 | 批量 + 容错 |
| T-13.3 | 后端 | 敏感词预检标记高危评价（词库来源待 A3） | FR-4 | 命中标记 |
| T-13.4 | 前端 | 审核后台新增「举报处理」Tab、批量勾选、敏感词标记 | T-13.1~13.3 | 工作台增强 |

---

## 6. 埋点（跨 FR-9~FR-13，主笔：产品助手）

| 任务 ID | 类型 | 描述 | 依赖 | 验收（PRD §7/§8） |
|---|---|---|---|---|
| T-M.1 | 后端 | Node 内置 `fetch` 埋点上报模块（不引 npm，端点待 A1） | NFR-1 | 零依赖上报 |
| T-M.2 | 前端 | 接入 `page_view` / `guide_generate` / `guide_source_click` / `rag_config_view` / `review_helpful` / `review_report` | PRD §7.1 | 事件齐备、字段合规 |
| T-M.3 | 后端双写 | 服务端写 `review_publish` / `review_moderate` / `report_resolve` | T-M.1 | 关键动作双写 |
| T-M.4 | 合规 | 不含明文密码 / 评价正文；去重、会话级；失败兜底 | T-M.1~M.3 | 隐私红线守住 |

---

## 7. 任务依赖总览
```
FR-8(Issue#1) ──┬─> FR-9 (T-9.1→9.2→9.3)
                   └─> FR-10 (T-10.1→10.2)        [依赖 FR-9]

FR-3 + FR-6 ──> FR-11 (T-11.1→11.2→{11.3, 11.4[A4]})
              └─> FR-12 (T-12.1→12.2→{12.3, 12.4})
                              │
FR-4 + FR-12 ──> FR-13 (T-13.1→13.2→{13.3[A3], 13.4})

埋点 T-M.* 横切 FR-9/10/11/12/13（依赖 A1 端点）
```

## 8. 风险清单
| 风险 | 等级 | 说明 | 缓解 |
|---|---|---|---|
| 埋点端点未定（A1） | 高 | 阻塞全部 T-M.* | 技术负责人优先确认上报端点 / 是否接分析平台 |
| 敏感词词库未定（A3） | 中 | 阻塞 T-13.3 | 先以静态词表占位，后续可接接口 |
| 排序权重未定（A4） | 中 | 影响 T-11.4 口径 | 先 1:1 叠加，避免与 `feedback.json` 冲突 |
| 存储结构未定（A5） | 中 | 影响 T-11.1 / T-12.1 | 沿用 NFR-4（gitignore、不引依赖），建议扩展 `reviews.json` 字段 |
| 隐私合规（A6） | 中 | 匿名 `page_view` 等告知 | 随 PRD 一并确认，埋点不含隐私红线字段 |
| FR-8 未接入 | ✅ 已闭环（Issue #1 关闭 / PR #2 合并） | RAG 可用前提明确 | — |

---

## 9. 流转提醒
- 本拆解文档随 PR #2 一并评审；评审确认后，可将 T-* 转为研发子事项分配给开发同学（附 PRD / 本拆解）。
- 阻塞项（A1~A6、Issue #1）需对应负责人先确认，再排细粒度工期。
- 重要文档（PRD、本拆解、REQUIREMENTS.md）上传项目资料库形成统一上下文。
